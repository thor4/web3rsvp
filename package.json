{
  "name": "web3rsvp",
  "version": "1.0.0",
  "description": "a web3-native eventbrite, except attendees need to deposit ETH to rsvp and will get it back upon checking in at the event",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "script": "node scripts/run.js",
    "deploy": "npx hardhat run scripts/deploy.js --network mumbai"
  },
  "keywords": [
    "events"
  ],
  "author": "bryan conklin",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.10.1"
  },
  "dependencies": {
    "dotenv": "^16.0.1"
  }
}
